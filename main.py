from fakepinterest import app

# O "if" só será executado quando o arquivo "main.py" for executado
if __name__ == "__main__":
    app.run(debug=True) # qualquer alteração no código será implementado no site, quando este estiver no ar




